name: ci

on:
  push:
    branches: [development]
  pull_request:
    branches: [development]

jobs:
  build-and-test:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Install Qt (pick correct arch on Windows)
      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.8.3"
          cache: true
          arch: ${{ runner.os == 'Windows' && 'win64_msvc2022_64' || '' }}

      # Ensure Ninja exists (Linux/macOS)
      - name: Install Ninja (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y ninja-build

      - name: Install Ninja (macOS)
        if: runner.os == 'macOS'
        run: brew install ninja

      # Ensure MSVC toolchain is active on Windows
      - name: Setup MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      # Configure / Build / Test (run explicitly from repo root)
      - name: Configure (CMake)
        working-directory: ${{ github.workspace }}
        run: cmake --preset ${{ runner.os == 'Windows' && 'windows-release' || 'release' }}

      - name: Build
        working-directory: ${{ github.workspace }}
        run: cmake --build --preset ${{ runner.os == 'Windows' && 'windows-release' || 'release' }}

      # Linux/macOS (single-config)
      - name: Run tests (Linux/macOS)
        if: runner.os != 'Windows'
        working-directory: ${{ github.workspace }}
        run: ctest --preset release --output-on-failure

      # Windows (multi-config) - MUST specify configuration matching your build
      - name: Run tests (Windows)
        if: runner.os == 'Windows'
        working-directory: ${{ github.workspace }}
        run: ctest --preset windows-release -C Release --output-on-failure

